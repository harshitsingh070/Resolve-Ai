# ResolveAI — Backend (FastAPI + SQLite + Groq) — Locked for Phase 2
# Install: python -m pip install -r requirements.txt

# Core API
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.7
pydantic-settings==2.9.1

# DB
sqlalchemy==2.0.41
# SQLite is stdlib (no extra driver needed)

# LLM
groq==0.19.0
httpx==0.28.1

# Utilities
python-dotenv==1.0.1

# Testing (Phase 4+)
pytest==8.3.5
pytest-asyncio==0.26.0
httpx==0.28.1
